const numbers = [1, 2, 3, 4, 5];

const joined = numbers.join(',');
console.log( joined );

const message  = "This is my first message";
const combined = message.split(' ');
const joinded  = combined.join('_');
console.log(combined);
console.log( joinded );